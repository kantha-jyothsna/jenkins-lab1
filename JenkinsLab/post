pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building..."
            }
        }
    }

    post {
        always {
            echo "Pipeline finished."
        }

        success {
            echo "Build Successful!"
        }

        failure {
            echo "Build Failed!"
        }
    }
}
